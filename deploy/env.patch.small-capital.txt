# ── Append to ~/apex_trader/.env (missing production + small-capital settings) ──
# After editing: docker compose restart api

# ICB — prevents SAFE_MODE lock on slow compliance checks
ICB_TIMEOUT_SEC=25.0
ICB_MAX_TRADES_PER_HOUR=20
ICB_TRADE_WINDOW_MINUTES=60

# Autonomous — dynamic trending watchlist
WATCHLIST_MODE=dynamic
AUTONOMOUS_AUTO_START=false
AUTONOMOUS_SESSION_START=09:20
AUTONOMOUS_SESSION_END=15:15
AUTONOMOUS_SCAN_INTERVAL_SEC=120
AUTONOMOUS_SYMBOL_COOLDOWN_SEC=300
AUTONOMOUS_INTER_SYMBOL_DELAY_SEC=1.0
AUTONOMOUS_MAX_SYMBOLS_PER_CYCLE=25
AUTONOMOUS_MAX_WATCHLIST_SIZE=80
AUTONOMOUS_UNIVERSE_POOL_SIZE=80
AUTONOMOUS_UNIVERSE_MAX_QUOTES=150
AUTONOMOUS_UNIVERSE_MIN_PRICE=30
AUTONOMOUS_UNIVERSE_MIN_VOLUME=40000
# ₹3.9k cash — if Kite margins net ≈ 50k (collateral), sync picks it up automatically.
# Set 0 to scan all liquid names; or cap e.g. 5000 when buying_power is high.
AUTONOMOUS_UNIVERSE_MAX_PRICE=0

# Match real Zerodha cash (sync from Kite every 5 min is source of truth)
INITIAL_CAPITAL=3900
# Only if /api/kite/margins shows net≈3900 but app shows ~50k MIS buying power:
# MIS_SIZING_LEVERAGE=12.8
